# System Architecture Diagram

## Complete Vehicle Tracking System Flow

```
┌──────────────────────────────────────────────────────────────────────┐
│                     IoT DEVICE / VEHICLE                             │
│                                                                      │
│  ┌────────────────────────────────────────────────────────────┐    │
│  │ Sensors:                                                   │    │
│  │ • Impact Sensor (Detects collision)                        │    │
│  │ • MPU6050 (Accelerometer + Gyroscope)                     │    │
│  │ • GPS Module (Location data)                               │    │
│  │                                                             │    │
│  │ Data sent every 5-10 seconds                               │    │
│  └────────────────────────────────────────────────────────────┘    │
└──────────────────────────┬───────────────────────────────────────────┘
                           │
                    sends data via
                     HTTP/Firebase
                           │
                           ↓
┌──────────────────────────────────────────────────────────────────────┐
│          FIREBASE REALTIME DATABASE (Cloud)                          │
│                                                                      │
│  {                                                                  │
│    "Device": "Safe" or "Alert",                                    │
│    "Impact": "Safe" or "Alert",                                    │
│    "LOC_IMPACT": "Safe_https://maps.google.com/?q=28.7,77.1",     │
│    "LOC_MPU6050": "Safe_https://maps.google.com/?q=28.7,77.1",    │
│    "MPU6050": "Safe" or "Alert",                                  │
│    "timestamp": "2026-02-23T10:30:00Z"                            │
│  }                                                                  │
│                                                                      │
│  🔐 Security Rules:                                                 │
│  {                                                                  │
│    "rules": {                                                       │
│      ".read": true,    ← reads allowed                             │
│      ".write": false   ← writes blocked                            │
│    }                                                                │
│  }                                                                  │
└──────────────────────────┬───────────────────────────────────────────┘
                           │
                    REST API call
                  (requests.get)
                           │
                           ↓
┌──────────────────────────────────────────────────────────────────────┐
│              DJANGO APPLICATION (Backend)                            │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────┐       │
│  │                   accident_detection/settings.py         │       │
│  │                                                          │       │
│  │  FIREBASE_DATABASE_URL = 'https://...' ← Configured    │       │
│  │  FIREBASE_API_KEY = 'AIzaSyC7...'      ← Configured    │       │
│  └──────────────────────────────────────────────────────────┘       │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────┐       │
│  │                     myapp/views.py                        │       │
│  │                                                          │       │
│  │  Function: fetch_firebase_vehicle_data()                │       │
│  │  • Makes REST API call to Firebase                       │       │
│  │  • Includes API key for authentication                   │       │
│  │  • Handles 401 errors gracefully                         │       │
│  │  • Returns parsed JSON data                              │       │
│  │                                                          │       │
│  │  Function: extract_coordinates_from_url()               │       │
│  │  • Extracts GPS coordinates from Google Maps URLs        │       │
│  │  • Returns {latitude, longitude}                         │       │
│  │                                                          │       │
│  │  @login_required                                         │       │
│  │  def vehicle_live_status(request):                       │       │
│  │    → Renders vehicle_live_status.html template           │       │
│  │    → Context: vehicle_data dict with all info            │       │
│  │                                                          │       │
│  │  @login_required                                         │       │
│  │  def vehicle_live_status_api(request):                   │       │
│  │    → Returns JsonResponse with vehicle_data              │       │
│  │                                                          │       │
│  └──────────────────────────────────────────────────────────┘       │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────┐       │
│  │              accident_detection/urls.py                  │       │
│  │                                                          │       │
│  │  path('vehicle-tracking/',       → vehicle_live_status  │       │
│  │  path('api/vehicle-status/',     → vehicle_live_status_api
│  │                                                          │       │
│  └──────────────────────────────────────────────────────────┘       │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────┐       │
│  │          templates/vehicle_live_status.html             │       │
│  │                                                          │       │
│  │  • Displays Impact Status (Safe/Alert) with color      │       │
│  │  • Displays Device Status                               │       │
│  │  • Shows GPS coordinates (Latitude, Longitude)          │       │
│  │  • Embedded Google Map iframe                           │       │
│  │  • Direct link to Google Maps                           │       │
│  │  • Error messaging with setup instructions              │       │
│  │                                                          │       │
│  └──────────────────────────────────────────────────────────┘       │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────┐       │
│  │      myapp/management/commands/test_firebase.py         │       │
│  │                                                          │       │
│  │  Management Command: python manage.py test_firebase     │       │
│  │                                                          │       │
│  │  • Tests Firebase connection                            │       │
│  │  • Tries with API key (Test 1)                          │       │
│  │  • Tries without auth (Test 2)                          │       │
│  │  • Displays vehicle data if successful                  │       │
│  │  • Shows error solutions if failed                      │       │
│  │                                                          │       │
│  └──────────────────────────────────────────────────────────┘       │
└──────────────────────────┬───────────────────────────────────────────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
        ↓                  ↓                  ↓
┌───────────────┐ ┌──────────────┐ ┌────────────────┐
│   USER BROWSER            │ MOBILE APP   │ OTHER CLIENTS
│  (HTML Page)              │ (JSON API)   │ (API Calls)
│                           │              │
│ http://localhost:8000/    │ http://local │
│ vehicle-tracking/         │ host:8000/   │
│                           │ api/vehicle- │
│ ┌────────────────────┐   │ status/      │
│ │ Impact Status:     │   │              │
│ │ ✅ Safe            │   │ Returns:     │
│ │                    │   │ {            │
│ │ Device Status:     │   │  "status":   │
│ │ ✅ Safe            │   │  "success",  │
│ │                    │   │  "data": {   │
│ │ GPS: 28.7, 77.1   │   │   "impact":  │
│ │                    │   │   "Safe",    │
│ │ [Google Maps]      │   │   "device":  │
│ │ 📍 View on Maps    │   │   "Safe",    │
│ │                    │   │   "lat": 28. │
│ │ ✅ All Clear!      │   │   "lng": 77. │
│ │                    │   │   "alert":   │
│ └────────────────────┘   │   false      │
│                           │  }           │
│                           │ }            │
│ Status: 200 OK            │ Status: 200  │
│ Auth: ✅ Required         │ Auth: ✅     │
│                           │ Required     │
└───────────────────────────┴──────────────┴────────────────┘
```

## Data Flow Sequence

```
1. IoT Device sends data to Firebase:
   Device → Firebase {Device: "Safe", Impact: "Safe", ...}

2. User/App requests vehicle status:
   Browser → Django: GET /vehicle-tracking/

3. Django fetches data from Firebase:
   Django → Firebase: GET /.json?key=AIzaSyC7...

4. Firebase returns data:
   Firebase → Django: {Device: "Safe", Impact: "Safe", ...}

5. Django processes data:
   • Extracts GPS from URL: "https://maps.google.com/?q=28.7,77.1"
   • Parses to: {latitude: 28.7, longitude: 77.1}
   • Checks alert status: Safe? Yes → alert: false

6. Django renders template:
   • Displays status cards (Impact, Device, MPU6050)
   • Creates Google Maps iframe
   • Shows GPS coordinates
   • Displays alert notification

7. User sees:
   Dashboard with real-time vehicle tracking data
   Interactive map showing location
   Status indicators (Safe/Alert)
```

## Component Interaction

```
┌─ REQUEST ─┐
│           ↓
│   ┌──────────────────┐
│   │ Django URL Router│
│   └──────┬───────────┘
│          │
│    match: /vehicle-tracking/
│          │
│   ┌──────↓───────────┐
│   │ vehicle_live_   │
│   │ status(request)  │
│   └──────┬───────────┘
│          │
│   ┌──────↓───────────────────────────┐
│   │ fetch_firebase_vehicle_data()     │
│   │ (calls Firebase REST API)         │
│   └──────┬───────────────────────────┘
│          │
│   ┌──────↓───────────────────────────┐
│   │ extract_coordinates_from_url()    │
│   │ (parses GPS data)                 │
│   └──────┬───────────────────────────┘
│          │
│   ┌──────↓────────────────────┐
│   │ Build vehicle_data dict   │
│   │ with processed data        │
│   └──────┬────────────────────┘
│          │
│   ┌──────↓─────────────────────────┐
│   │ render() with template          │
│   │ (vehicle_live_status.html)      │
│   └──────┬─────────────────────────┘
│          │
│   ┌──────↓──────────┐
│   │ HTML Response   │
│   └─ RESPONSE ─────┘
```

## Error Handling Flow

```
┌──────────────────────────────┐
│ fetch_firebase_vehicle_data()│
└──────────┬───────────────────┘
           │
      NO ERRORS?
      /          \
    YES          NO
     │            │
     │      ┌──────────────┐
     │      │ Check Status │
     │      └──────┬───────┘
     │             │
     │        WHAT STATUS?
     │        /    |    \
     │      200  401   OTHER
     │       │     │     │
     │       │     │    ┌──┴────────────────────┐
     │       │     │    │ Log Error, Return     │
     │       │     │    │ vehicle_data with     │
     │       │     │    │ error message         │
     │       │     │    └───────────┬───────────┘
     │       │     │                │
     │       │   ┌─┴───────────────────┴────┐
     │       │   │ Try public access       │
     │       │   │ (no API key)            │
     │       │   └─┬─────────────────────┬─┘
     │       │     │                     │
     │       │   WORKS?               FAILS?
     │       │     │                     │
     │       │     │              ┌──────↓──────┐
     │       │     │              │ Return     │
     │       │     │              │ error      │
     │       │     │              └──────┬──────┘
     │       │     │                     │
     │       │     └─────────┬───────────┘
     │       │               │
     │       └───────┬───────┘
     │               │
     ├──────────────→ Parse Response
                     │
              ┌──────↓──────────┐
              │ Extract Data:   │
              │ • Impact        │
              │ • Device        │
              │ • Location      │
              │ • Coordinates   │
              └──────┬──────────┘
                     │
              ┌──────↓────────────────┐
              │ Build vehicle_data    │
              │ Context for Template  │
              └──────────────────────┘
```

---

**System Status:** ✅ All Components Ready  
**Awaiting:** Firebase Security Rules Configuration  
**Time to Complete:** ~2 minutes (just update rules)

🚀 Once Firebase rules are set, the entire system will be operational!
